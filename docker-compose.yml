version: '3'
services:
    app:
        build: ./
        command: pnpm run dev --host
        ports:
            - 5173:5173
        volumes:
            - ./:/app/
            - nodemodules:/app/node_modules

volumes:
    nodemodules: {}